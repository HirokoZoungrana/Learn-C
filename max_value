#include<stdio.h>

#define N 10

int main(void){
  int max, num[N];
  int i;

  for(i=0; i<N; i++){
    scanf("%d", &num[i]);
  }

  max = 0;
  for(i=0; i<N; i++){
    if(num[i] > max) max = num[i];
  }
  printf("max value :%d\n", max);

  return 0;
}
