#include<stdio.h>

int main(void){
  int num[10] = {0};
  int i, sum;
  double ave;

  for(i=0; i<10; i++){
    scanf("%d", &num[i]);
  }
  sum = 0;
  for(i=0; i<10; i++){
    sum += num[i];
  }

  ave = (double)sum/10;

  printf("%.1f\n", ave);
  return 0;
}
