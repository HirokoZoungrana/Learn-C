#include<stdio.h>

int main(void){
  int num;
  int i;

  scanf("%d", &num);
  for(i=2; i<=(num/2); i++){
    if((num % i)==0) break;
  }
  if(i>(num/2)) printf("%d is Prime Number\n", num);
  else printf("%d is not Prime Number\n", num);

  return 0;
}
